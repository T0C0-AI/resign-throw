# source — 원본 소스 & 개발 파일 (백업)

게임에 직접 쓰이진 않지만 보존용으로 둔 원본/개발 파일입니다.

- `originals/` : 캐릭터·이펙트·배경의 **원본 고해상도 PNG** (편집·재압축의 출발점)
- `preview.html` : 개발용 미리보기. 시안 배지가 붙은 버전이며, 배포본은 루트 `index.html`.

배포에 쓰이는 압축본(webp)은 `../assets/`에 있습니다.
원본 → 리사이즈 → webp 변환으로 `assets/`를 생성합니다.
